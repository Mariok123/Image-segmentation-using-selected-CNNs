{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Python with argument",
            "type": "shell",
            //"command": "python train.py -m UNET -e 3 -l",
            "command": "python predict.py -m DoubleUNET -s data/pred_images",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}