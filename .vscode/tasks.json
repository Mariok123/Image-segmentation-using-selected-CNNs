{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Python with argument",
            "type": "shell",
            "command": "python train.py -m 3 -d 2 -e 30 -es -sv",
            //"command": "python predict.py -m 3 -l ./ResUNETpp_20230416T031524.pth.tar -s data/pred_images",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}